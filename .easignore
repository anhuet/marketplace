# Infrastructure (not needed for mobile build)
infra/
.sfdx/

# Force EAS to run expo prebuild fresh (generates ios/ from app.json + plugins)
apps/mobile/ios/

# Build artifacts
*.tsbuildinfo
package-lock.json

# IDE & OS
.idea/
.vscode/
.DS_Store

# CDK output
cdk.out/

# Docs (not needed for build)
docs/
.tasks/
.agents/
